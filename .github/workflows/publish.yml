name: Publish

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: |
          ./gradlew publish
        env:
          PKG_USERNAME: ${{ secrets.PKG_USER }}
          PKG_PASSWORD: ${{ secrets.PKG_PW }}